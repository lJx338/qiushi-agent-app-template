# starter-app 岗位任务规格

状态：待填写。负责人：lJx338。默认样板不是完整业务需求。

- 用户与要解决的问题：
- 本次范围/非目标：
- 输入/输出、金额/单位/舍入与业务状态规则：
- 角色、记录/字段权限、源调用身份、模型与导出用途（见 PERMISSIONS.md）：
- 动作/schema/副作用、审批与幂等：
- 页面与 loading/empty/error/forbidden/核对中状态：
- 独立业务验收样例与预期值：
- 平台能力依赖：已提供、开发模拟、缺失接口与负责人：
- 允许修改路径、工作分支、基线 commit、Node/npm 版本：
- 验证证据与未完成项：

不要粘贴真实客户数据或凭证。依赖未实现的生产能力必须标注阻塞，不用 mock 作为生产验收。
